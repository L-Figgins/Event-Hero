// import { fromJS } from 'immutable';
// import { selectEventShowcaseDomain } from '../selectors';

describe('selectEventShowcaseDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
