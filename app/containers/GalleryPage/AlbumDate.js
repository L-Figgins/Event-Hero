import styled from 'styled-components';

const AlbumDate = styled.div`
  display: flex;
  justify-content: flex-end;
  align-items: flex-end;
  color: white;
  font-size: 0.75rem;
  font-weight: bold;
  text-transform: uppercase;
`;

export default AlbumDate;
